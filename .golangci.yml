run:
  skip-dirs:
    - vendor

  skip-files:

linters:
  disable-all: true
  enable:
    - gofmt
    - deadcode
    - errcheck
    - gofmt
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - structcheck
    - typecheck
    - unused
    - varcheck

